<%= simple_form_for([@post , @comment]) do |f| %>
  

  <div class="field">
    <%= f.input :name %>
  </div>
  <div class="field">
    <%= f.input :content %>
  </div>
  <div class="actions">
   <%= f.submit  %>
  </div>
<% end %>